# Equity Liquidity Pipeline Placeholder

This directory is reserved for stock-level price, volume, liquidity, and market-cap inputs used by the passive market instability research notebook.

The first notebook version can use `yfinance` price and volume data with most-recent market capitalization repeated through history as a clearly labeled simplification.

Place raw files under `data/raw/` and cleaned research panels under `data/processed/`.
